app = "opportunities-agent"
primary_region = "ord"

[build]

[env]
  DB_PATH = "/data/opportunities.db"

# No [[services]] — this is a background worker, not a web server

[[mounts]]
  source = "opportunities_data"
  destination = "/data"

[processes]
  app = "python main.py"
